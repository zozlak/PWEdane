--------------------
Zmiany w PWEdane 0.0.6 (2015-04-28)

- Dostosowanie do zmiany w strukturze grupy danych "skale" pakietu ZPD

--------------------
Zmiany w PWEdane 0.0.5 (2015-04-08)

- Argument punktuj funkcji pobierz_wyniki() jest teraz brany pod uwagę
  (dotychczas, wskutek błędu, jego wartość była pomijana).

--------------------
Zmiany w PWEdane 0.0.4 (2015-03-31)

- Pobieranie wyników niewypunktowanych wymusza niestosowanie skali.

--------------------
Zmiany w PWEdane 0.0.3 (2015-03-23)

- Dodanie do funkcji pobierz_dane_kontekstowe() zmiennej "populacja" 
  określającej, czy dana obserwacja należy do podstawowej populacji PWE, 
  czy nie

--------------------
Zmiany w PWEdane 0.0.2 (2015-03-19)

- Zamykanie otwartych połączeń z bazą danych przy opuszczaniu funkcji (tam, 
  gdzie to możliwe), aby oszczędzać pulę połączeń serwera baz danych.
- Usunięcie wyświetlania komunikatów generowanych przez funkcje join_...()

--------------------
Zmiany w PWEdane 0.0.1 (2015-03-18)

- Dodanie funkcji:
  - pobierz_wyniki() do pobierania wyników egzaminów
  - pobierz_zrownywanie() do pobierania wyników testów zrównujących
  - znajdz_skale_dla_testow() do wyszukiwania domyślnej skali dla w.w. funkcji
  - pobierz_dane_kontekstowe() do pobierania danych kontekstowych dla w.w. 
    funkcji
- Zmiany techniczne:
  - obłożenie podstawowymi testami funkcji pobierz_zrownywanie(), 
    pobierz_dane_kontekstowe(), znajdz_skale_dla_testow()
  - próba uruchomienia Travis-a i Coveralls